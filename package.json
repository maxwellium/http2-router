{
  "name": "http2-router",
  "version": "0.2.0",
  "description": "",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "files": [
    "build/src"
  ],
  "author": "maxwellium",
  "repository": "github:maxwellium/http2-router",
  "license": "MIT",
  "keywords": [],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsc -p .",
    "cert": "cd test && openssl req -x509 -newkey rsa:2048 -nodes -sha256 -subj '/CN=localhost' -keyout localhost-privkey.pem -out localhost-cert.pem"
  },
  "devDependencies": {
    "@types/node": "^11.10.4",
    "gts": "^0.9.0",
    "typescript": "~3.1.0"
  }
}
